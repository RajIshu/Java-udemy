package com.JavaMasterclass;

// We can write 'ICanFly' instead of 'CanFly' here to represent that it's an Interface
// but, 'ICanFly' gives very different meaning than 'CanFly' and even it's not mandatory
// to append 'I' at the start in case of Interfaces in Java
public interface CanFly {
    void fly();
}
