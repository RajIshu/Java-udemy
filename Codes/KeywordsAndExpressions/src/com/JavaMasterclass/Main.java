package com.JavaMasterclass;

public class Main {

    public static void main(String[] args) {
	// Keywords
        // Java has 51 reserved words that used as keywords.

        // We cannot use reserved words as variable in our program.
        // int int = 5; // Uncomment it
        int int2 = 5;


    // Expressions
        // 1 mile = 1.609344 Kilometres
        double kilometres = (100 * 1.609344); // Km in 100 miles
        /*
        Here, Expression ==> kilometres = (100 * 1.609344) // Operands + Operators
              Keyword ==> double

        So,   Statement = Keyword + Expression
              Statement ==> double kilometres = (100 * 1.609344);

        NOTE: Semicolon is not part of the expression.

        Example:
            a) if(highScore == 50) {
                    System.out.println("This is an expression");
               }
               Expressions ==> highScore == 50
                          ==> "This is an expression"

            b) int score = 100;
               if (score > 90) {
                    System.out.println("You got the high score");
                    score = 0;
               }
               Expressions ==> score = 100
                          ==> score > 90
                          ==> "You got the high score"
                          ==> score = 0
        */

    }
}
